name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # build:
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout Repository
  #     uses: actions/checkout@v2

  #   - name: Set up JDK
  #     uses: actions/setup-java@v2
  #     with:
  #       distribution: 'adopt'
  #       java-version: '20'
        
  #   - name: Make Gradlew Executable
  #     run: chmod +x ./gradlew

  #   - name: Build with Gradle
  #     run: ./gradlew build

  release:
    # needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '20'

    - name: Create Release and upload
      uses: ncipollo/release-action@v1.13.0
      with:
        actifacts: "/app/build/outputs/apk/release/app-release-unsigned.apk"
        body: |
          Uploading initial release
        
    
